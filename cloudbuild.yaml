# Build API image from repo root (see root Dockerfile).
steps:
  - name: gcr.io/cloud-builders/docker
    args:
      - build
      - '-t'
      - 'gcr.io/$PROJECT_ID/inboxpilot-backend:$BUILD_ID'
      - .

images:
  - 'gcr.io/$PROJECT_ID/inboxpilot-backend:$BUILD_ID'
