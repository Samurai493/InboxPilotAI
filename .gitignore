# Virtual environments at repo root (backend/.gitignore only applies under backend/)
.venv/
venv/
env/
ENV/

# Local env
.env
.env.local

# OS / editor
.DS_Store
Thumbs.db

# Cursor — local/agent skills (often machine-specific or duplicated from ~/.cursor)
.cursor/skills/

# Ad-hoc debug logs at repo root
debug-*.log

# Root-level docs (context packs, session notes); keep out of remote if you prefer local-only
/docs/
