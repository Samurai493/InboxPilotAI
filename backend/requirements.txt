# Core FastAPI dependencies (FastAPI must allow Starlette with CVE fixes, e.g. CVE-2025-62727 → starlette>=0.49.1)
fastapi==0.128.0
starlette>=0.49.1,<1.1.0
# Loose pin: strict == can conflict with extras resolution; LangChain stack may pull httpx newer than 0.25.x
uvicorn[standard]>=0.24.0,<0.35.0
pydantic==2.10.6
# langchain-community 0.4.x requires pydantic-settings>=2.10.1,<3 (2.7.x conflicts with pip resolver).
pydantic-settings==2.11.0

# Database
sqlalchemy==2.0.38
alembic==1.12.1
psycopg2-binary==2.9.11
redis==5.0.1

# LangGraph and LangChain
# langgraph 0.2.x capped langgraph-checkpoint<3 (CVE-2025-64439, CVE-2026-27794 → checkpoint>=4).
# LangGraph 1.1.x requires langgraph-prebuilt → langchain-core>=1.0; align the whole stack to 1.x.
langgraph==1.1.3
langgraph-checkpoint>=4.0.0,<5.0.0
langchain-core==1.2.23
langchain==1.2.13
langchain-openai==1.1.12
langchain-anthropic==1.4.0
langchain-community==0.4.1
langchain-google-genai==4.2.1
langsmith>=0.1.112,<1.0.0

# Gmail API — google-api-core 1.x pins google-auth<2; 2.x matches google-auth 2.x (pip + pip-audit).
google-api-core==2.24.2
google-auth==2.49.1
google-auth-oauthlib==1.3.0
google-auth-httplib2==0.3.0
google-api-python-client==2.193.0

# Utilities
python-dotenv==1.0.0
python-multipart==0.0.22
httpx>=0.25.0,<0.29.0
PyJWT==2.12.1
slowapi==0.1.9
cryptography>=46.0.6,<47.0.0

# Development
pytest==7.4.3
pytest-asyncio==0.21.1
black==26.3.1
isort==8.0.1
